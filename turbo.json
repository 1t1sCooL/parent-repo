{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "build/**"]
    },
    "web-app#build": {
      "dependsOn": ["^build"],
      "outputs": [],
      "cache": true
    }
  }
}